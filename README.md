# Dependencies
* bundler
* ruby 2.7.0
* rspec

# Getting Started
- Switch to ruby 2.7.0 (install it if you don't have it).
- gem install bundler
- bundle install

# Running tests

`bundle exec rspec`
